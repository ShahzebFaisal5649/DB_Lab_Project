# You can use the below commands for check your database and tables in the MySQL in terminal:

show databases; //Lets you show all the database in the main terminal.
use <database_name>; // In this case it will be edu_connect_db

# After this you can enter inside your database and can use your SQL commands such as below:
show tables;
SELECT * FROM <table_name>;